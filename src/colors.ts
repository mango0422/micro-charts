/**
 * Color utilities entry point
 * Import from '@.../micro-charts/colors' to include only color utilities
 */

export {
  defaultColors,
  generateColorPalette,
  hexToRgb,
  interpolateColor,
  rgbToHex,
} from './core/colors';

export type { RGB } from './types';
